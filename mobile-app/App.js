/**
 * Entry point exigido pelo Expo (node_modules/expo/AppEntry.js procura por
 * um App.js na raiz do projeto). A implementação real vive em src/App.js,
 * conforme a estrutura de arquivos definida na documentação de arquitetura.
 */
export { default } from './src/App';
